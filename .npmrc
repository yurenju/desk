# openapi-typescript@7 declares a peer of typescript@^5.x, but this project
# pins typescript@~6.0.2. The generated types work fine under TS 6; this just
# lets npm (including `npm ci` in CI) accept the locked resolution instead of
# failing with ERESOLVE.
legacy-peer-deps=true
